# Route 114 fossil-tunnel visible-language cleanup

The Fossil Maniac tunnel still used four untouched English conversations, including a visible statement that DEVON researches reviving Pokémon from fossils.

This micro-lot localizes only those four existing text blocks. The fossil checks, Root/Claw Fossil item IDs, revived-fossil flag, post-game cave opening, metatiles and every executable script command remain unchanged. The research reference is reinterpreted as Consórcio Horizonte while the Fossil Maniac keeps the same structural role and opinions.

No new location name or fossil/species identity is invented. The validator checks localized anchors, rejects the inherited English/DEVON fragments and enforces a maximum of 32 visible characters per segment.

GitHub Actions is intentionally not used while the monthly quota is exhausted; the text-only source diff is small enough for direct inspection.
